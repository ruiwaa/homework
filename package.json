{
  "scripts": {
    "start": "live-server . --port=4000 --host=localhost --no-browser",
    "format": "prettier --write .",
    "dev":"vite",
     "build": "vite build",
    "preview": "vite preview",
    "remove": "rm -rf dist"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "prettier": "^3.6.2",
    "vite": "^7.3.0"
  }
}
